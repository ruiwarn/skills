# {{CHIP_NAME}} 用户手册索引

## 📁 资料文件

| 文件 | 说明 | 用途 |
|------|------|------|
| `register_index.md` | 寄存器快速索引 | **首选** - 快速定位寄存器行号 |
| `manual.md` | 完整手册正文 | 用 offset 精准读取 |
| `manual.pdf` | 原始PDF | 备份验证 |

---

## 🔍 检索策略

### 查寄存器
1. 先在 `register_index.md` 搜索寄存器名或地址
2. 找到行号后，用 `Read(manual.md, offset=行号-10, limit=80)` 读取详情

### 查功能说明
- 用 Grep 在 `manual.md` 搜索关键词
- 常用关键词：
  - 寄存器：`0x` + 地址
  - 接口：`I2C`、`SPI`、`UART`
  - 中断：`IRQ`、`IE`、`IF`

---

## 📑 主要章节速查

> 运行 generate_index.py 后自动填充

| 章节 | 内容 | 行号范围 |
|------|------|----------|
| ... | ... | ... |

---

## 🏷️ 常用寄存器速查

> 运行 generate_index.py 后自动填充

| 功能 | 寄存器 | 地址 | 行号 |
|------|--------|------|------|
| ... | ... | ... | ... |
